# 開発環境用Dockerfile
FROM node:20-alpine

# 作業ディレクトリの設定
WORKDIR /app

# 依存関係のインストールに必要なパッケージ
RUN apk add --no-cache libc6-compat

# 依存関係ファイルをコピー
COPY package.json package-lock.json* ./

# 依存関係のインストール
RUN npm ci

# アプリケーションコードをコピー（ボリュームマウントで上書きされる）
COPY . .

# Prisma Clientの生成
RUN npx prisma generate

# ポートの公開
EXPOSE 3000

# 開発サーバーの起動（docker-compose.dev.ymlでオーバーライド可能）
CMD ["npm", "run", "dev"]
